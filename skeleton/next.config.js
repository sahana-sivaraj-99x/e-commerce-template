/** @type {import('next').NextConfig} */

const nextConfig = {
	images: {
		domains: ["fakestoreapi.com", "images.unsplash.com","nextjs-ecommerce-two-ashen.vercel.app"],
	},
};

module.exports = nextConfig;
